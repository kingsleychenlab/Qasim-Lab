# Full pipeline dependencies (step01 -> step13 + audits).
# Verified against every import in code/*.py.

# Dataset access + EEG I/O (step01-03, step07, audits)
requests>=2.31
mne>=1.6

# Core numerics / tables (all steps)
numpy>=1.24
pandas>=2.0

# T5-large word embeddings (step04)
torch>=2.1
transformers>=4.35
sentencepiece>=0.1.99
tqdm>=4.65

# Ridge decoding + held-out cross-validation (step08)
scikit-learn>=1.3

# Memory model: logistic mixed-effects + p-values (step11)
statsmodels>=0.14
scipy>=1.10

# Figures (step13)
matplotlib>=3.7

ZOOM OPENING SCRIPT  (~60-90 seconds — read at the start)

Thanks everyone for joining. Today I'll walk through a project called
"AI Word-Embedding Fidelity and Memory."

Here's the question in one sentence: when someone studies a word, is that word
more likely to be remembered if their brain activity during encoding more
accurately predicts the AI language model's representation of that word?

The idea builds on a 2025 Nature Communications paper showing you can partly
decode which word a person is reading from EEG by mapping the brain signal onto
a language model's word embedding. Our twist was to add a memory outcome: not
just "can we decode the word," but "does decoding it well predict whether it's
later recalled."

I'll take you through the whole pipeline — the AI embeddings, the EEG window, the
ridge-regression decoding, and the final mixed-effects memory model. I'll spend
real time on validation, because we audited every stage and the whole thing is
independently reproducible.

I'll be upfront now about where we land: the result is a clean, honest null. The
memory prediction was not supported. But the method is solid, fully audited, and
it points clearly to what to try next. Let's dig in.
